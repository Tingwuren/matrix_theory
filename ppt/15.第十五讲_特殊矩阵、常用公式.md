矩阵分析与应用

第十五讲 特殊矩阵、常用公式

# 信息与通信工程学院 吕旌阳

本讲主要内容

正定矩阵

не 非负矩阵

- 常用公式

## 正定矩阵

定义:令 $\mathrm{A}$ 是 $\mathrm{n}$ 阶Hermite矩阵,若对任意复 $\mathrm{n}$ 维列向量 $\mathrm{x} \neq  0$ ,都有

$$
{\mathrm{x}}^{\mathrm{H}}\mathrm{{Ax}} \geq  0
$$

则称 $A$ 为Hermite非负定矩阵，简称为非负定矩阵。

对任意复n维列向量 $\mathrm{x} \neq  0$ ，都有

$$
{\mathrm{x}}^{\mathrm{H}}\mathrm{{Ax}} > 0
$$

称 $\mathrm{A}$ 为Hermite正定矩阵,简称为正定矩阵

性质(1):若 $\mathrm{A}$ 正定，且 $\mathrm{k}$ 为正常数，则 $\mathrm{k}$ A正定

性质(2):若 $\mathrm{A}$ 、 $\mathrm{B}$ 正定，则 $\mathrm{A} + \mathrm{B}$ 正定

性质 (3): 若 $\mathrm{A}$ 正定,则 ${\mathrm{A}}^{-1}$ 正定

## 正定矩阵判断准则

Hermite矩阵A是正定矩阵因为如果 $\mathrm{{Ax}} = \lambda \mathrm{x}$ ,则有

$$
0 < {\mathrm{x}}^{\mathrm{H}}\mathrm{{Ax}} = {\mathrm{x}}^{\mathrm{H}}{\lambda }_{\mathrm{X}} = \lambda {\mathrm{x}}^{\mathrm{H}}\mathrm{x} = \lambda \parallel \mathrm{x}{\parallel }^{2}
$$

一定有 $\lambda  > 0$ 。反之,必然存在酉矩阵 $\mathrm{U}$ ,使得

$$
\mathbf{\Lambda } = {\mathrm{{UAU}}}^{\mathrm{H}},\;\mathbf{\Lambda } = \operatorname{diag}\left( {{\mathbf{\lambda }}_{1},\cdots ,{\mathbf{\lambda }}_{\mathrm{n}}}\right)
$$

因为 ${\lambda }_{\mathrm{i}} > 0$ ,且对任意 $\mathrm{x} \neq  0$ 有 $\mathrm{U}\mathrm{x} = \mathrm{y} \neq  0$

从而 ${x}^{H}{Ax} = {\left( {U}^{H}y\right) }^{H}A\left( {{U}^{H}y}\right)  = {y}^{H}{UA}{U}^{H}y = {y}^{H}{\Lambda y} > 0$

所以A是正定的

$$
= \sum {\lambda }_{\mathrm{i}}{\left| {y}_{\mathrm{i}}\right| }^{2}
$$

Hermite矩阵A是非负定矩阵 A的特征值都为非负数

## 正定矩阵判断准则

Hermite矩阵A是正定矩阵(非负定)

A的特征值都为正数(非负数)

tr A $> {\lambda }_{\mathrm{i}}\left( \mathrm{A}\right) ,\;\left( {\text{ or tr A } \geq  {\lambda }_{\mathrm{i}}\left( \mathrm{A}\right) }\right) \;\mathrm{i} = 1,\cdots ,\mathrm{n}$

$\mathrm{A} = {\mathrm{P}}^{\mathrm{H}}\mathrm{P}\;\mathrm{P}$ 是 $\mathrm{n}$ 阶非奇异矩阵(矩阵)

$\mathrm{A} = {\mathrm{B}}^{2}\;\mathrm{\;B}$ 是 $\mathrm{n}$ 阶正定矩阵 (非负定矩阵)

$\mathrm{A} = {\mathrm{C}}^{\mathrm{H}}\mathrm{{BC}}\;\mathrm{C}$ 是 $\mathrm{n} \times  \mathrm{m}$ 列满秩矩阵, $\mathrm{B}$ 正定

A的n个顺序主子式为正数(非负数)

证明: Hermite矩阵 $A$ 是正定矩阵

$\mathrm{A} = {\mathrm{P}}^{\mathrm{H}}\mathrm{P}\;\mathrm{P}$ 是 $\mathrm{n}$ 阶非奇异矩阵

$$
\mathrm{A} = {\mathrm{B}}^{2}\;\mathrm{\;B}\text{ 是 }\mathrm{n}\text{ 阶正定矩阵 }
$$

如果 $\mathrm{A}$ 正定，则存在酉矩阵 $\mathrm{U}$ ,使得

$$
\mathrm{A} = \mathrm{U}\operatorname{diag}\left( {{\lambda }_{1},{\lambda }_{2},\cdots ,{\lambda }_{\mathrm{n}}}\right) {\mathrm{U}}^{\mathrm{H}}
$$

$$
= \mathrm{U}\operatorname{diag}\left( {\sqrt{{\lambda }_{1}},\sqrt{{\lambda }_{2}},\cdots ,\sqrt{{\lambda }_{\mathrm{n}}}}\right) \operatorname{diag}\left( {\sqrt{{\lambda }_{1}},\sqrt{{\lambda }_{2}},\cdots ,\sqrt{{\lambda }_{\mathrm{n}}}}\right) {\mathrm{U}}^{\mathrm{H}}
$$

$$
= \mathrm{U}\operatorname{diag}\left( {\sqrt{{\lambda }_{1}},\sqrt{{\lambda }_{2}},\cdots ,\sqrt{{\lambda }_{\mathrm{n}}}}\right) {\mathrm{U}}^{\mathrm{H}}\mathrm{U}\operatorname{diag}\left( {\sqrt{{\lambda }_{1}},\sqrt{{\lambda }_{2}},\cdots ,\sqrt{{\lambda }_{\mathrm{n}}}}\right) {\mathrm{U}}^{\mathrm{H}}
$$

令 $P = \operatorname{diag}\left( {\sqrt{{\lambda }_{1}},\sqrt{{\lambda }_{2}},\cdots ,\sqrt{{\lambda }_{n}}}\right) {U}^{H}\;$ 即有 $A = {P}^{H}P$

令 $B = U\operatorname{diag}\left( {\sqrt{{\lambda }_{1}},\sqrt{{\lambda }_{2}},\cdots ,\sqrt{{\lambda }_{n}}}\right) {U}^{H}$ 即有 $A = {B}^{2}$

## 正定矩阵判断准则

BA, C是正定矩阵, 且AC=CA, 则AC为正定矩阵

$$
\because {\left( \mathrm{{AC}}\right) }^{\mathrm{H}} = {\mathrm{C}}^{\mathrm{H}}{\mathrm{A}}^{\mathrm{H}} = \mathrm{{CA}} = \mathrm{{AC}}
$$

$\mathrm{{AC}}$ 是Hermite矩阵,因为 $\mathrm{A}$ 正定,所以有 $\mathrm{A} = {\mathrm{B}}^{2}$ , $\mathrm{B}$ 正定

$$
{\mathrm{B}}^{-1}\left( \mathrm{{AC}}\right) \mathrm{B} = \mathrm{{BCB}}
$$

即 $\mathrm{{AC}}$ 和 $\mathrm{{BCB}}$ 有相同特征值，而且

$$
\mathrm{{BCB}} = {\mathrm{B}}^{\mathrm{H}}\mathrm{{CB}}
$$

BCB 的特征值全部为正数，所以AC为正定矩阵

BA是正定矩阵, C是非负定矩阵, 且AC=CA, 则AC为非负定矩阵

## 正定矩阵的常用不等式

BA, B是n阶Hermite矩阵, 若A-B为正定矩阵(非负定矩阵), 则称 $A$ 大于 $B$ ,记为 $A > B.\left( {A\text{ 不小于 }B\text{ 记 }A \geq  B}\right)$

也可记为 $\mathrm{B} < \mathrm{A}.\left( {\mathrm{B} \leq  \mathrm{A}}\right)$

$\mathrm{A} \geq  \mathrm{B}$ 的充要条件是,对任意复 $\mathrm{n}$ 维列向量 $\mathrm{x}$ 都有

$$
{\mathbf{x}}^{\mathrm{H}}\mathbf{A}\mathbf{x} \geq  {\mathbf{x}}^{\mathrm{H}}\mathbf{B}\mathbf{x}
$$

对 $A, B$ 是实对角阵时, $A = \operatorname{diag}\left( {{a}_{1},{a}_{2},\cdots ,{a}_{n}}\right)$

$B = \operatorname{diag}\left( {{b}_{1},{b}_{2},\cdots ,{b}_{n}}\right)$ ,则有 ${a}_{i} \geq  {b}_{i}$

## 矩阵不等式的性质

1. 传递性 $\;\mathrm{A} \geq  \mathrm{B},\mathrm{B} \geq  \mathrm{C} \Rightarrow  \mathrm{A} \geq  \mathrm{C}$

2. 线性性 $\mathrm{A} \geq  \mathrm{B},\mathrm{k} > 0 \Rightarrow  \mathrm{{kA}} \geq  \mathrm{{kB}}$

$$
{A}_{1} \geq  {B}_{1},{A}_{2} \geq  {B}_{2} \Rightarrow  {A}_{1} + {A}_{2} \geq  {B}_{1} + {B}_{2}
$$

3. $\mathrm{n}$ 阶Hermite矩阵 $\mathrm{A},\mathrm{B}$ 有 $\mathrm{A} \geq  \mathrm{B}$ ,且 $\mathrm{P} \in  {\mathrm{C}}^{\mathrm{n} \times  \mathrm{m}}$ 则 ${\mathrm{P}}^{\mathrm{H}}\mathrm{{AP}} \geq  {\mathrm{P}}^{\mathrm{H}}\mathrm{{BP}}$

4. 若矩阵 $\mathrm{A}$ 为非负定矩阵,则 $\mathrm{A} \leq  \left( {\mathrm{{tr}}\mathrm{A}}\right) \mathrm{I}$

## 矩阵不等式的性质

5. 若矩阵 $A, B$ 为 $n$ 阶正定矩阵,且 ${AB} = {BA}, A \geq  B$

则 ${A}^{2} \geq  {B}^{2}$

6. 若 $A, B$ 都为正定矩阵,则 $A > B \Leftrightarrow  {B}^{-1} > {A}^{-1}$

证明: 由于 $A$ 为正定,从而 ${A}^{-1}$ 正定,存在可逆矩阵 $C$

使得 ${A}^{-1} = C{C}^{H}\;A = {\left( C{C}^{H}\right) }^{-1} = {\left( {C}^{H}\right) }^{-1}{C}^{-1}$

因此由 $\mathrm{A} - \mathrm{B}$ 正定可得 $\mathrm{I} - {\mathrm{C}}^{\mathrm{H}}\mathrm{{BC}}$ 正定，推得 ${\mathrm{C}}^{\mathrm{H}}\mathrm{{BC}}$ 的特征值都小于 1, 从而 ${\left( {\mathrm{C}}^{\mathrm{H}}\mathrm{{BC}}\right) }^{-1}$ 的特征值都大于 1,即 ${\left( {\mathrm{C}}^{\mathrm{H}}\mathrm{{BC}}\right) }^{-1} - \mathrm{I}$ 的特征值都大于 0 从而 ${\left( {\mathrm{C}}^{\mathrm{H}}\mathrm{{BC}}\right) }^{-1} - \mathrm{I}$ 正定,即 ${\mathrm{C}}^{-1}{\mathrm{\;B}}^{-1}{\left( {\mathrm{C}}^{\mathrm{H}}\right) }^{-1} - \mathrm{I}$ 正定

因此 ${\mathrm{B}}^{-1} - {\mathrm{{CC}}}^{\mathrm{H}}$ 正定。反之亦然

## 非负矩阵

定义:若 $n$ 阶实矩阵 $A = \left( {a}_{ij}\right)$ 满足 ${a}_{ij} \geq  0$ ，则 $A$ 为非负矩阵； 若 ${a}_{ij} > 0$ ,则 $A$ 为正矩阵。

定义: 若存在置换矩阵 $P$ 使得 ${PA}{P}^{\mathrm{T}} = \left( \begin{matrix} {A}_{11} & {A}_{12} \\  0 & {A}_{22} \end{matrix}\right)$

则称 $A$ 为可约矩阵; 否则为不可约矩阵

定理7.1: 若 $\mathrm{A} \in  {\mathrm{R}}^{\mathrm{n} \times  \mathrm{n}}$ 为非负不可约矩阵,则

1). 谱半径 $\rho \left( \mathrm{A}\right)$ 为 $\mathrm{A}$ 的特征值;

2). 存在属于 $\rho \left( A\right)$ 的正特征向量;

3) $A$ 的任意元素增加时, $\rho \left( A\right)$ 不减少。

推论: 若 $A$ 为正矩阵,则

1). 谱半径 $\rho \left( A\right)$ 为 $A$ 的特征值;

2). 存在属于 $\rho \left( A\right)$ 的正特征向量;

3). $A$ 的任意元素增加时, $\rho \left( A\right)$ 不减少。

定理 7.2 若 $\mathrm{A} \in  {\mathrm{R}}^{\mathrm{n} \times  \mathrm{n}}$ 为非负矩阵,则

1). 谱半径 $\rho \left( \mathrm{A}\right)$ 为 $\mathrm{A}$ 的特征值;

2). 存在属于 $\rho \left( A\right)$ 的非负特征向量;

3) $A$ 的任意元素增加时, $\rho \left( A\right)$ 不减少。

例 : $A = \left( \begin{array}{lll} 1 & 0 & 1 \\  0 & 1 & 1 \\  1 & 3 & 1 \end{array}\right) , B = \left( \begin{array}{lll} 1 & 0 & 1 \\  {0.1} & 1 & 1 \\  1 & 3 & 1 \end{array}\right)$

$$
\rho \left( A\right)  = 3,\;\rho \left( B\right)  = {3.0365}
$$

## Toeplitz矩阵

定义: $A = \left( \begin{matrix} {a}_{0} & {a}_{-1} & {a}_{-2} & \cdots & {a}_{-n + 1} \\  {a}_{1} & {a}_{0} & {a}_{-1} & \cdots & {a}_{-n + 2} \\  {a}_{2} & {a}_{1} & {a}_{0} & \cdots & {a}_{-n + 3} \\  \vdots & \vdots & \vdots &  \ddots  & \vdots \\  {a}_{n - 1} & {a}_{n - 2} & {a}_{n - 2} & \cdots & {a}_{0} \end{matrix}\right)$ 称为Toeplitz矩阵

可简记为 $A = {\left( {a}_{i - j}\right) }_{i, j = 1}^{n}$

## Hilbert矩阵 (条件数差的典型例子)

$H = \left\lbrack  \begin{matrix} 1 & 1/2 & \cdots & 1/n \\  1/2 & 1/3 & \cdots & 1/\left( {n + 1}\right) \\  \cdots & \cdots & \cdots & \cdots \\  1/n & 1/\left( {n + 1}\right) & \cdots & 1/\left( {{2n} - 1}\right)  \end{matrix}\right\rbrack   = {\left( \frac{1}{i + j - 1}\right) }_{i, j = 1}^{n}$ 行列式: $\det H = \frac{{\left\lbrack  1!2!\cdots \left( n - 1\right) !\right\rbrack  }^{3}}{n!\left( {n + 1}\right) !\cdots \left( {{2n} - 1}\right) !} \; {\left( {H}^{-1}\right) }_{ij} = \frac{{\left( -1\right) }^{i + j}\left( {n + i - 1}\right) !\left( {n + j - 1}\right) !}{\left( {i + j - 1}\right) !{\left\lbrack  \left( i - 1\right) !\left( j - 1\right) !\right\rbrack  }^{2}\left( {n - i}\right) !\left( {n - j}\right) !}$

$$
i, j = 1,2,\ldots , n\text{ ; }
$$

## Hadamard矩阵

如果n阶矩阵H的元素为+1或-1,且满足

$$
H{H}^{\mathrm{T}} = \mathrm{{nI}}
$$

则称H为Hadamard矩阵.

性质:若n>2且n阶Hadamard矩阵存在，则n为4的倍数。

猜想: 若n为4的倍数, 则n阶Hadamard矩阵存在.

$$
{H}_{1} = \left\lbrack  1\right\rbrack  \;{H}_{2} = \left\lbrack  \begin{matrix} 1 & 1 \\  1 &  - 1 \end{matrix}\right\rbrack
$$

$$
{\mathrm{H}}_{{2}^{k}} = \left\lbrack  \begin{array}{ll} {\mathrm{H}}_{{2}^{k - 1}} & {\mathrm{H}}_{{2}^{k - 1}} \\  {\mathrm{H}}_{{2}^{k - 1}} &  - {\mathrm{H}}_{{2}^{k - 1}} \end{array}\right\rbrack   = {\mathrm{H}}_{2} \otimes  {\mathrm{H}}_{{2}^{k - 1}}\;\text{ 即Sylvester方法 }
$$

## 矩阵的直和

定义: 若 $\mathrm{A} \in  {\mathrm{C}}^{\mathrm{m} \times  \mathrm{m}},\mathrm{B} \in  {\mathrm{C}}^{\mathrm{n} \times  \mathrm{n}}$ ,则 $\mathrm{A}$ 与 $\mathrm{B}$ 的直和记为

$$
A \oplus  B \in  {C}^{\left( {m + n}\right)  \times  \left( {m + n}\right) }\text{ ,定义为 }
$$

$$
A \oplus  B = \left\lbrack  \begin{matrix} A & {0}_{m \times  n} \\  {0}_{n \times  m} & B \end{matrix}\right\rbrack
$$

类似的, 有

$$
\mathrm{B} = \mathop{\bigoplus }\limits_{{\mathrm{i} = 0}}^{{\mathrm{N} - 1}}{\mathrm{A}}_{\mathrm{i}} = {\mathrm{A}}_{0} \oplus  {\mathrm{A}}_{1} \oplus  \cdots  \oplus  {\mathrm{A}}_{\mathrm{N} - 1} = \operatorname{diag}\left( {{\mathrm{A}}_{0},{\mathrm{A}}_{1},\cdots ,{\mathrm{A}}_{\mathrm{N} - 1}}\right)
$$

Hadamard积

定义: 若 $A = {\left\lbrack  {a}_{ij}\right\rbrack  }_{n \times  n}, B = {\left\lbrack  {b}_{ij}\right\rbrack  }_{n \times  n}$ ,则 $A$ 与 $B$ 的Hadamard积记为 $A \odot  B \in  {C}^{n \times  n}$ ，定义为

$$
A \odot  B = \left\lbrack  {{a}_{\mathrm{{ij}}}{b}_{\mathrm{{ij}}}}\right\rbrack
$$

也称为Schur积或者elementwise product

定理: 若 $\mathrm{A},\mathrm{B} \in  {\mathrm{C}}^{\mathrm{m} \times  \mathrm{m}}$ 是正定的(半正定的),则其Hadamard积也是正定的(半正定的)

定理: 若 $A, B, C \in  {C}^{m \times  n}$ 矩阵,且 $1 = {\left\lbrack  1,1,\cdots ,1\right\rbrack  }^{T}$ 是求和向量,

$D = \operatorname{diag}\left( {{d}_{1},{d}_{2},\cdots ,{d}_{m}}\right)$ ,其中 ${d}_{i} = \mathop{\sum }\limits_{{j = 1}}^{n}{a}_{ij}$ ,则有

$$
\operatorname{tr}\left( {{A}^{T}\left( {B \odot  C}\right) }\right)  = \operatorname{tr}\left( {\left( {{A}^{T} \odot  {B}^{T}}\right) C}\right)
$$

$$
{1}^{T}{A}^{T}\left( {B \odot  C}\right) 1 = \operatorname{tr}\left( {{B}^{T}{DC}}\right)
$$

## Hadamard积的性质

1. 若 $\mathrm{A},\mathrm{B} \in  {\mathrm{C}}^{\mathrm{m} \times  \mathrm{m}}$ ,则

$$
A \odot  B = B \odot  A
$$

$$
{\left( A \odot  B\right) }^{T} = {A}^{T} \odot  {B}^{T}
$$

$$
{\left( A \odot  B\right) }^{H} = {A}^{H} \odot  {B}^{H}
$$

$$
{\left( A \odot  B\right) }^{ * } = {A}^{ * } \odot  {B}^{ * }
$$

2. 若 $A \in  {C}^{m \times  m}$ ,则 $A \odot  {O}_{m \times  n} = {O}_{m \times  n} \odot  A = {O}_{m \times  n}$

3. 若 $c$ 为常数,则 $c\left( {A \odot  B}\right)  = \left( {cA}\right)  \odot  B = A \odot  \left( {cB}\right)$

4. 若 $A \in  {C}^{m \times  m}$ ,则 $A \odot  {I}_{m} = {I}_{m} \odot  A = \operatorname{diag}\left( A\right)$

$$
= \operatorname{diag}\left( {{a}_{11},{a}_{22},\cdots ,{a}_{nn}}\right)
$$

5. 若 $\mathrm{A},\mathrm{B},\mathrm{C},\mathrm{D} \in  {\mathrm{C}}^{\mathrm{m} \times  \mathrm{m}}$ ，则

$$
A \odot  B \odot  C = A \odot  \left( {B \odot  C}\right)  = \left( {A \odot  B}\right)  \odot  C
$$

$$
\left( {A \pm  B}\right)  \odot  C = A \odot  C \pm  B \odot  C
$$

$$
\left( {A + B}\right)  \odot  \left( {C + D}\right)  = A \odot  C + A \odot  D + B \odot  C + B \odot  D
$$

6. 若 $\mathrm{A},\mathrm{C} \in  {\mathrm{C}}^{m \times  m},\mathrm{B},\mathrm{D} \in  {\mathrm{C}}^{n \times  n}$ 则

$$
\left( {\mathrm{A} \oplus  \mathrm{B}}\right)  \odot  \left( {\mathrm{C} \oplus  \mathrm{D}}\right)  = \left( {\mathrm{A} \odot  \mathrm{C}}\right)  \oplus  \left( {\mathrm{B} \odot  \mathrm{D}}\right)
$$

7. $\operatorname{tr}\left( {{A}^{T}\left( {B \odot  C}\right) }\right)  = \operatorname{tr}\left( {\left( {{A}^{T} \odot  {B}^{T}}\right) C}\right)$

## Kronecker积

定义: 若 $\mathrm{A} \in  {\mathrm{C}}^{\mathrm{m} \times  \mathrm{n}},\mathrm{B} \in  {\mathrm{C}}^{\mathrm{p} \times  \mathrm{q}}$ ,则 $\mathrm{A}$ 与 $\mathrm{B}$ 的右Kronecker积

$A \otimes  B$ 定义为

$$
A \otimes  B = \left\lbrack  {{a}_{ij}B}\right\rbrack   = \left\lbrack  \begin{matrix} {a}_{11}B & {a}_{12}B & \cdots & {a}_{1n}B \\  {a}_{21}B & {a}_{22}B & \cdots & {a}_{2n}B \\  \vdots & \vdots & & \vdots \\  {a}_{m1}B & {a}_{m2}B & \cdots & {a}_{mn}B \end{matrix}\right\rbrack
$$

定义: 若 $\mathrm{A} \in  {\mathrm{C}}^{\mathrm{m} \times  \mathrm{n}},\mathrm{B} \in  {\mathrm{C}}^{\mathrm{p} \times  \mathrm{q}}$ ,则 $\mathrm{A}$ 与 $\mathrm{B}$ 的左Kronecker积

$$
\begin{array}{l} \mathrm{A} \otimes  \mathrm{B}\text{ 定义为 } \\  \mathrm{A} \otimes  \mathrm{B} = \left\lbrack  {\mathrm{{Ab}}}_{\mathrm{{ij}}}\right\rbrack   = \left\lbrack  \begin{matrix} {\mathrm{{Ab}}}_{11} & {\mathrm{{Ab}}}_{12} & \cdots & {\mathrm{{Ab}}}_{1\mathrm{q}} \\  {\mathrm{{Ab}}}_{21} & {\mathrm{{Ab}}}_{22} & \cdots & {\mathrm{{Ab}}}_{2\mathrm{q}} \\  \vdots & \vdots & & \vdots \\  {\mathrm{{Ab}}}_{\mathrm{p}1} & {\mathrm{{Ab}}}_{\mathrm{p}2} & \cdots & {\mathrm{{Ab}}}_{\mathrm{p}\mathrm{q}} \end{matrix}\right\rbrack  \\  \end{array}
$$

Kronecker积的性质

1. $\left( {\lambda \mathrm{A}}\right)  \otimes  \left( {\mu \mathrm{B}}\right)  = {\lambda \mu }\left( {\mathrm{A} \otimes  \mathrm{B}}\right)$ ,其中 $\lambda ,\mu  \in  \mathrm{C}$

2. $\left( {A + B}\right)  \otimes  C = A \otimes  C + B \otimes  C$

$A \otimes  \left( {B + C}\right)  = A \otimes  B + A \otimes  C$

3. $A \otimes  \left( {B \otimes  C}\right)  = \left( {A \otimes  B}\right)  \otimes  C$

4. ${\left( A \otimes  B\right) }^{T} = {A}^{T} \otimes  {B}^{T}\;{\left( A \otimes  B\right) }^{H} = {A}^{H} \otimes  {B}^{H}$

5. $\left( {A \otimes  B}\right) \left( {C \otimes  D}\right)  = \left( {AC}\right)  \otimes  \left( {BD}\right)$

6. 如果 $A, B$ 均为非奇异矩阵

$$
{\left( \mathrm{A} \otimes  \mathrm{B}\right) }^{-1} = {\mathrm{A}}^{-1} \otimes  {\mathrm{B}}^{-1}
$$

谢谢大家！